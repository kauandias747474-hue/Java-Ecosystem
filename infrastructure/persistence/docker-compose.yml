services:
  db:
    image: postgres:15
    container_name: postgres_pda
    environment:
      - POSTGRES_DB=pda_db
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=senha_segura
    ports:
      - "5432:5432"
